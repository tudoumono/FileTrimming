# ドキュメント処理パイプライン 詳細設計書 v2

作成日: 260311 101606
更新日: 260311 214114

---

## 0. 今回の見直しポイント

- OpenAI API で利用するモデル名の列挙が古くなっていたため、設計書上は「固定モデル名」ではなく「設定で切り替える前提」に整理した
- 対象拡張子として `.trf` を追加し、RTF 系の処理ルートを `.rtf/.trf` の両方で明記した
- Phase 3-6 の記述を、現行実装 (`steps/step2_normalize.py` から `steps/step6_chunk.py`) と整合する内容へ更新した
- Word/RTF の高度なレイアウト保持や BAGLES の高度パースは「将来拡張候補」と「現行実装」を分けて整理した
- 詳細設計書と拡張子別メモの役割分担を明文化し、現行実装の正本を本書へ統一した

### 0.1 文書の役割分担

- 本書は、現行実装の処理フロー、Phase ごとの仕様、ログ出力、フォールバック挙動の正本とする
- `docs/拡張子別/*.md` は、拡張子固有の論点、品質リスク、試験観点、将来拡張候補を整理する補足メモとする
- 本書と拡張子別メモで差分が生じた場合、現行挙動については本書を優先する
- 実装変更時は、まず本書を更新し、必要な場合のみ関連する拡張子別メモを追随更新する

---

## 1. 前提条件

### 1.1 環境

| 項目 | 内容 |
|------|------|
| 処理実行環境 | Windows |
| Office | Microsoft Office インストール済み。Word / Excel / PowerPoint の COM 利用を前提とする |
| Python | 3.10+ |
| RAG 基盤 | Dify（Docker セルフホスティング、AWS 上） |
| LLM プロバイダ | `openai` / `ollama` を切り替え可能。既定値は `openai` |
| OpenAI 接続設定 | `.env` の `OPENAI_API_KEY` `OPENAI_MODEL` `OPENAI_BASE_URL` で指定 |
| OpenAI モデル指定 | 現行既定値は `gpt-4o-mini`。ただし設計上は固定せず、利用可能なモデルへ差し替え可能とする |
| トークン上限 | `TOKEN_LIMIT` で指定。既定値は `80000` |

### 1.2 LLM / OpenAI 方針

- 現行コードは `llm/factory.py` と `llm/openai_client.py` により、Chat Completions 互換の呼び出しで OpenAI API を利用する
- OpenAI API 側の利用可能モデルは更新頻度が高いため、本設計書では `gpt-4o / gpt-4o-mini` のような固定列挙を中心にしない
- 2026-03-11 時点の OpenAI 公式ドキュメントでは GPT-5 系が前面に出ているが、本リポジトリの現行実装は `.env` の `OPENAI_MODEL` 差し替えで追従する前提とする
- Responses API への移行は未実施。現行仕様はあくまで Chat Completions 互換クライアント前提である

### 1.3 処理対象ファイル（7カテゴリ / 19拡張子）

| # | ファイルカテゴリ | 対象拡張子 | 内部分類 | 備考 |
|---|------------------|------------|----------|------|
| 1 | Excel | `.xlsx` `.xls` `.xlsm` | `excel` / `excel_legacy` | `.xls` は Phase 2 で `.xlsx` に正規化 |
| 2 | Word | `.docx` `.doc` | `word` / `word_legacy` | `.doc` は Phase 2 で `.docx` に正規化 |
| 3 | BAGLES | `.bik` `.bca` `.bci` `.bpg` | `bagles` | 提供済みサンプルで確認済みの拡張子 |
| 4 | RTF / TRF | `.rtf` `.trf` | `rtf` / `trf` | いずれも Word COM で `.docx` に正規化する |
| 5 | PDF | `.pdf` | `pdf` | テキスト抽出可否で品質差が出る |
| 6 | PowerPoint | `.pptx` `.ppt` | `pptx` / `pptx_legacy` | `.ppt` は Phase 2 で `.pptx` に正規化 |
| 7 | テキスト系 | `.txt` `.csv` `.tsv` `.md` `.log` | `text` | 文字コードをフォールバックしながら読み込む |

補足:

- 現行ソースには BAGLES 候補として `.bgl` `.bag` `.def` を扱う系統も残っている。設計上は `.bik/.bca/.bci/.bpg` を優先対象として扱う
- `.trf` は今回の見直しで対象拡張子として明示追加した

### 1.4 現時点の提供ファイル内訳（260件）

| 区分 | 件数 | 想定拡張子 | パイプライン上の扱い | 備考 |
|------|------|------------|----------------------|------|
| Docs | 129 | `.doc` | `word_legacy` | 提供元の表記は `Docs` |
| Excel | 32 | `.xls` | `excel_legacy` | レガシー Excel |
| Docx | 29 | `.docx` | `word` | 現行 Word |
| RTF | 25 | `.rtf` | `rtf` | Word 正規化ルートへ投入 |
| TRF | 0 | `.trf` | `trf` | 設計対象に追加。現時点の提供件数は 0 |
| XLSX | 19 | `.xlsx` | `excel` | 現行 Excel |
| Text | 14 | `.txt` | `text` | 現時点の提供は `.txt` 中心 |
| BIK | 8 | `.bik` | `bagles` | BAGLES |
| BCA | 1 | `.bca` | `bagles` | BAGLES |
| BCI | 1 | `.bci` | `bagles` | BAGLES |
| BPG | 1 | `.bpg` | `bagles` | BAGLES |
| PDF | 1 | `.pdf` | `pdf` | PDF |

- 合計 260 件
- Word 系は `Docs + Docx = 158件` に `RTF 25件` と `TRF 0件` を加え、設計対象としては 183 件で扱う
- Excel 系は `Excel + XLSX = 51件`
- BAGLES 系は `BIK + BCA + BCI + BPG = 11件`
- `.ppt/.pptx/.csv/.tsv/.md/.log` は現時点の提供件数 0 だが、処理ルートは設計対象に含める

---

## 2. 全体アーキテクチャ

```text
[入力フォルダ]
    |
    v
Phase 1: コピー & ファイル分類
  - 入力フォルダ配下を step1/files へコピー
  - 拡張子ベースでファイル種別を分類
  - classification.json を出力

    |
    v
Phase 2: フォーマット正規化
  - .doc/.rtf/.trf -> .docx (Word COM)
  - .xls -> .xlsx (Excel COM)
  - .ppt -> .pptx (PowerPoint COM)
  - その他はパススルー
  - normalization_log.json を出力

    |
    v
Phase 3: トークン推定 & 物理分割
  - ファイル種別ごとに簡易テキスト抽出
  - `TOKEN_LIMIT` 超過時のみ行単位で分割
  - split_log.json を出力

    |
    v
Phase 4: Markdown 変換
  - Excel -> xlwings
  - Word/PDF/PPT -> MarkItDown
  - RTF/TRF -> Step 2 で `.docx` 化できた場合は Word ルート、失敗時のみ `pandoc` フォールバック
  - BAGLES -> 簡易パーサ
  - 図や画像は [図N] プレースホルダへ置換
  - conversion_log.json を出力

    |
    v
Phase 5: 品質判定 & 構造化
  - Markdown 品質スコアリング
  - HIGH/MEDIUM -> ルールベース補正
  - LOW -> LLM 再構造化（LLM 未設定時はルールベース補正）
  - quality_log.json を出力

    |
    v
Phase 6: チャンキング & 出力
  - 見出しベースまたは固定サイズでチャンク化
  - YAML Front Matter 付き Markdown を出力
  - Dify 取り込み用ディレクトリを生成

    |
    v
[出力フォルダ] -> [Dify Knowledge Base]
```

---

## 3. Phase 別詳細設計

### 3.1 Phase 1: コピー & ファイル分類

実装対象:

- `steps/step1_copy.py`
- `utils/file_utils.py`

仕様:

- 入力フォルダ配下の全ファイルを、元のディレクトリ構造を保ったまま `work/step1/files` にコピーする
- 分類結果は `work/step1/classification.json` に `{種別: [相対パス...]}` 形式で保存する
- 現行実装は拡張子ベース分類が中心であり、magic bytes やヘッダ再判定は本流実装にはまだ入れていない

設計上の注意:

- 分類ルールは 1 箇所に集約する
- `.trf` を `trf` として扱う
- BAGLES の拡張子群は、提供実績ベースの `.bik/.bca/.bci/.bpg` と、旧実装由来の `.bgl/.bag/.def` の扱いを整理して統一する

### 3.2 Phase 2: フォーマット正規化

実装対象:

- `steps/step2_normalize.py`

正規化マッピング:

| 入力種別 | 変換先 | 実装 |
|----------|--------|------|
| `word_legacy` | `.docx` | Word COM |
| `rtf` | `.docx` | Word COM |
| `trf` | `.docx` | Word COM |
| `excel_legacy` | `.xlsx` | Excel COM |
| `pptx_legacy` | `.pptx` | PowerPoint COM |
| その他 | そのまま | コピー |

仕様:

- 変換成功時は `normalization_log.json` に `source` `source_type` `output` `output_type` `strategy` を記録する
- `.rtf/.trf` は `requires_visual_review=true` を付与し、図・フロー図・埋め込みオブジェクトの確認対象として残す
- 変換失敗時は原本コピーへフォールバックし、後段処理を継続する

設計判断:

- Windows + Office 前提のため、LibreOffice ではなく COM を採用する
- xlwings を採用している Excel ルートと整合が取りやすい

### 3.3 Phase 3: トークン推定 & 物理分割

実装対象:

- `steps/step3_split.py`
- `utils/token_utils.py`

現行仕様:

- `tiktoken.encoding_for_model("gpt-4o")` を使ってトークン推定する
- ここで使う `gpt-4o` は推定用エンコーダ指定であり、実際の推論モデル固定を意味しない
- `TOKEN_LIMIT` 以下、またはテキスト抽出結果が空の場合は分割せず原本を通過させる
- `TOKEN_LIMIT` 超過時のみ、行単位オーバーラップ分割を行い、分割後は `.txt` として保存する

簡易テキスト抽出ルート:

| 種別 | 抽出方法 |
|------|----------|
| `text` | `read_text_file()` |
| `excel` | `openpyxl` でセル値を列挙 |
| `word` | `python-docx` で段落を連結 |
| `pdf` | `PyMuPDF` でページテキストを抽出 |
| その他 | 文字列読み込みを試行し、失敗時は空文字 |

制約:

- 現行実装は「シート単位」「見出し単位」「定義書単位」のような意味分割は未実装
- 大きいファイルは最終的に行単位分割へ寄るため、将来的には Excel / Word / BAGLES の意味単位分割へ改善する

### 3.4 Phase 4: Markdown 変換

実装対象:

- `steps/step4_markdown.py`

変換ルート:

| 種別 | 変換方法 | 備考 |
|------|----------|------|
| `excel` | `xlwings` | `used_range` を Markdown テーブル化 |
| `word` | `MarkItDown` | `.docx` と、Step 2 で正規化済みの `.rtf/.trf` を含む |
| `pptx` | `MarkItDown` | PowerPoint |
| `pdf` | `MarkItDown` | PDF |
| `bagles` | 簡易パーサ | 定義書キーワードで区切ってコードブロック化 |
| `rtf` / `trf` | `pandoc` -> `MarkItDown` -> テキスト読み込み | Step 2 で `.docx` 化できなかった場合のフォールバック |
| `text` | そのまま読み込み | エンコーディングフォールバックあり |

共通後処理:

- Markdown / HTML 画像タグを検出し、`[図N]` または `[図N: alt]` プレースホルダへ置換する
- Step 2 の `normalization_log.json` を参照して `source_type` を引き継ぐ
- 変換結果は `conversion_log.json` に保存する
- `source_type` が `rtf` / `trf` の場合でも、正規化が成功していれば実際の変換器は `word -> MarkItDown` になる

設計上の位置づけ:

- [docx 取り扱いメモ](./拡張子別/docx.md) や [rtf 取り扱いメモ](./拡張子別/rtf.md) にある「Word COM + OOXML 抽出」「HTML table 保持」は将来拡張候補である
- 現時点の詳細設計書では、現行実装の主ルートを優先して記載する

### 3.5 Phase 5: 品質判定 & 構造化

実装対象:

- `steps/step5_structure.py`
- `llm/base.py`
- `llm/factory.py`
- `llm/openai_client.py`

品質判定ロジック:

- 共通: 制御文字比率、空文書、罫線ノイズを評価する
- Excel: Markdown テーブルの有無を重視する
- Word: 見出し構造と `[図N]` の数を見る
- BAGLES: `## チェック更新定義書` などのキーワードをボーナス判定に使う

処理方針:

| 品質 | 現行処理 |
|------|----------|
| HIGH | `rule_based_cleanup()` のみ |
| MEDIUM | `rule_based_cleanup()` のみ |
| LOW | LLM があれば再構造化、なければ `rule_based_cleanup()` |

LLM 呼び出し仕様:

- 呼び出し先は `LLMClient` 抽象へ隠蔽する
- `.env` の `LLM_PROVIDER` で `openai` / `ollama` を切り替える
- OpenAI 利用時は `OPENAI_MODEL` をそのまま利用する
- 現行既定値は `gpt-4o-mini` だが、設計としては任意の利用可能モデルへ差し替え可能とする

制約:

- Step 4 で元拡張子情報が一部失われるため、Step 5 はファイル名ベースで元種別を推定している
- MEDIUM 品質での「部分的 LLM 補正」は未実装

### 3.6 Phase 6: チャンキング & Dify 向け出力

実装対象:

- `steps/step6_chunk.py`

仕様:

- `CHUNK_MAX_TOKENS=2000`
- `CHUNK_MIN_TOKENS=100`
- `## ` 見出しがあれば見出し単位、それ以外は固定サイズ + オーバーラップで分割する
- 出力先は `output/{doc_id}/` とし、`{doc_id}_chunkNNN.md` を生成する
- 各チャンク先頭に以下の Front Matter を付与する

```yaml
---
source_file: <step5 からの相対パス>
doc_id: <ファイル stem>
chunk_index: <0 始まり>
total_chunks: <総チャンク数>
chunk_title: <見出し名。なければ空文字>
---
```

- 完了時は `output/{doc_id}/.done` を作成する

---

## 4. BAGLES 定義書の扱い

### 4.1 現在の前提

- 提供済みサンプルでは `.bik` `.bca` `.bci` `.bpg` の 4 拡張子を BAGLES として扱う
- 現行 Markdown 変換は、高度な表構造パースではなく「定義書キーワードで区切って Markdown 化する簡易ルート」である
- 検証用出力は `work/_validation` 配下に存在し、BAGLES の基本通過ルートは確認済み

### 4.2 将来拡張候補

- 富士通ツールからのテキスト / HTML / CSV / PDF エクスポートを優先する
- 直接解析が必要な場合は、定義書種別ごとに専用パーサを追加する
- 大型定義書では「定義書単位」や「モジュール単位」での意味分割を追加する

---

## 5. 実装上の課題と整理事項

1. BAGLES の拡張子判定がソース上で一元化されていない
2. `.trf` は設計対象に追加したが、実データでの変換品質確認は未実施
3. Phase 3 の分割はまだ意味分割ではなく、行単位分割が主である
4. Step 5 が元ファイル種別をファイル名推定しているため、判定の安定性が低い
5. Word / RTF の構造保持抽出は拡張子別メモにあるが、本流実装には未反映
6. OpenAI 側はモデル更新が早いため、設計書に特定モデル名をハードコードし続けない運用が必要
7. OpenAI の Responses API へ移行する場合は、現行の Chat Completions 互換実装との差分整理が必要

---

## 6. 次のステップ

1. `utils/file_utils.py` の判定表を整理し、`.trf` と BAGLES 拡張子群を単一の定義へ集約する
2. Step 4 から Step 5 へ `source_type` を明示的に引き継ぎ、ファイル名推定をやめる
3. Excel / Word / BAGLES の意味単位分割を Phase 3 に追加する
4. 実データの `.trf` が入手でき次第、Word COM 正規化と Markdown 化品質を検証する
5. OpenAI 利用時のモデル選定基準を「高精度」「低コスト」「ローカル代替」に分けて運用ルール化する
6. Responses API へ移行するかどうかを、機能要件と SDK 差分を見たうえで別途判断する

---

## 7. 参考資料

### OpenAI

1. OpenAI API Models
   - https://developers.openai.com/api/docs/models
   - 2026-03-11 時点で GPT-5 系が前面に出ていることを確認

2. OpenAI API Reference: Responses
   - https://developers.openai.com/api/reference/resources/responses/methods/create
   - 現在の主要 API が Responses API であることを確認

3. OpenAI API Reference: Chat
   - https://developers.openai.com/api/reference/resources/chat
   - 現行実装が Chat Completions 互換である整理に利用

### 拡張子別メモ

4. [拡張子別メモ README](./拡張子別/README.md)
5. [docx 取り扱いメモ](./拡張子別/docx.md)
6. [rtf 取り扱いメモ](./拡張子別/rtf.md)
7. [BAGLES 定義書メモ](./拡張子別/bagles.md)

### BAGLES / 富士通関連

8. Open BAGLES 販売開始（富士通プレスリリース）
   - https://pr.fujitsu.com/jp/news/2007/02/14-2.html

9. BAGLES II 支援事例（富士通プレスリリース）
   - https://pr.fujitsu.com/jp/news/2013/06/3.html

10. Open BAGLES 技術資料 PDF
   - https://www.fujitsu.com/downloads/JP/archive/imgjp/group/fmcs/services/purpose/bagles-open-doc.pdf
