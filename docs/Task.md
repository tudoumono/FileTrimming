# チャンキング検討タスク整理

## 1. この文書の目的

- この文書は、チャンキング検討における設計判断の正本として扱う
- 前提条件、これまでに試した内容、現時点で分かっていることを記録として残し、その上で次に考えるべきことを整理する
- 議論の結果はこの文書のチェックリスト（§6）に決定事項として記録し、蓄積する
- `docs/ドキュメント処理パイプライン詳細設計書_v2.md` は設計ドラフトとして残し、本文書の議論結果が出揃った段階で再構成する
- `docs/拡張子別/*.md` は拡張子ごとの補足メモとして扱う

## 2. 固定前提

- Dify 実行環境は、完全オフラインの閉域網でセルフホストされる
- Dify のバージョンは `1.13.0`
- Dify の配備構成は `Windows host + WSL + Docker Compose`
- ドキュメントの整形、構造化、チャンキングは、別のオンライン設置可能 PC で事前実施する
- 前処理後の成果物をオフライン環境へ持ち込み、まずは `Dify UI` で手動取り込みする
- そのため、最終成果物は「Dify に取り込めること」「Dify UI で取り込めること」を最優先条件とする
- Dify の接続先 LLM は `Ollama` 経由のローカルモデル群を前提とする
- 回答モデルは現時点で `GPT-OSS 120B` を利用している
- 埋め込みモデルは `Qwen` 系の埋め込みモデルを利用している認識だが、正式なモデル名は後で再確認する
- 仮置きの Python 実装は一旦撤去しており、現時点では `docs/` 配下の文書を正本として議論を進める
- 内部ドキュメントそのものは共有できない前提とする
- そのため、今後の議論や検討では、口頭での説明、要約、匿名化した説明、または近い性質のサンプル文を使って進める
- `docs/ドキュメント処理パイプライン詳細設計書_v2.md` は設計ドラフトとして参照用に残す。議論結果が出揃った段階で本文書の決定事項を反映して再構成する
- `docs/拡張子別/*.md` は拡張子ごとの補足メモとして扱う

### 2.1 処理対象ファイル（7カテゴリ / 19拡張子）

| # | ファイルカテゴリ | 対象拡張子 | 備考 |
|---|------------------|------------|------|
| 1 | Excel | `.xlsx` `.xls` `.xlsm` | `.xls` は `.xlsx` に正規化 |
| 2 | Word | `.docx` `.doc` | `.doc` は `.docx` に正規化 |
| 3 | BAGLES | `.bik` `.bca` `.bci` `.bpg` | 提供済みサンプルで確認済みの拡張子 |
| 4 | RTF / TRF | `.rtf` `.trf` | いずれも Word COM で `.docx` に正規化する |
| 5 | PDF | `.pdf` | テキスト抽出可否で品質差が出る |
| 6 | PowerPoint | `.pptx` `.ppt` | `.ppt` は `.pptx` に正規化 |
| 7 | テキスト系 | `.txt` `.csv` `.tsv` `.md` `.log` | 文字コードをフォールバックしながら読み込む |

### 2.2 現時点の提供ファイル内訳（260件）

| 区分 | 件数 | 想定拡張子 | 備考 |
|------|------|------------|------|
| Docs | 129 | `.doc` | 提供元の表記は `Docs` |
| Excel | 32 | `.xls` | レガシー Excel |
| Docx | 29 | `.docx` | 現行 Word |
| RTF | 25 | `.rtf` | Word 正規化ルートへ投入 |
| TRF | 0 | `.trf` | 設計対象に追加。現時点の提供件数は 0 |
| XLSX | 19 | `.xlsx` | 現行 Excel |
| Text | 14 | `.txt` | 現時点の提供は `.txt` 中心 |
| BIK | 8 | `.bik` | BAGLES |
| BCA | 1 | `.bca` | BAGLES |
| BCI | 1 | `.bci` | BAGLES |
| BPG | 1 | `.bpg` | BAGLES |
| PDF | 1 | `.pdf` | PDF |

- 合計 260 件
- Word 系は `Docs + Docx = 158件` に `RTF 25件` を加え、設計対象としては 183 件
- Excel 系は `Excel + XLSX = 51件`
- BAGLES 系は `BIK + BCA + BCI + BPG = 11件`

## 3. これまでに試したこと

### 3.1 目的

- まずは Dify に登録できる状態に持っていくことを優先した
- 文書ごとの細かな性質差はまだ吸収せず、全形式を同一の流れで前処理した

### 3.2 実施した流れ

1. 各種ドキュメントを `MarkItDown` で Markdown に変換した
2. OpenAI API を使い、Markdown 内のノイズ除去、ゴミ情報の削除、崩れた表の整形を行った
3. 整形後の内容をもう一度 OpenAI に渡し、可能な限り書き下した文章へ変換した
4. Dify 側で改行を使って分割されることを意識し、1文ずつ扱いやすい形へ寄せた

### 3.3 この方法で意図していたこと

- 形式差をいったん無視し、Dify に入れやすい形へそろえる
- 表や箇条書きを、そのままではなく検索しやすい文章へ寄せる
- まずは「Dify で使えるか」を確認するためのベースラインを作る

### 3.4 具体例

- Excel にエラーコード表がある場合は、各行の内容を
- `エラーコード〇〇はこういう意図のエラーです`
- `このエラーの時にはこういう対処をしてください`
- `こういう条件で判定しています`
- のような文章へ書き換えた

## 4. 現時点で分かっていること

- 一律処理だけでは品質を十分に考慮できない
- Excel 系ではある程度機能しても、`Docs / Docx`、特に `RTF / TRF`、`BAGLES` ではうまくいかないケースが多い
- 「整形して自然文にすること」と「どの単位で区切るか」は、本来は別の論点である
- 今回の一律処理は、最終版の戦略ではなく、まず Dify に登録するための暫定ベースラインとして扱うのが妥当
- 今後は、形式ごとに別の処理方針を考える必要がある

## 5. 今回の議論で守る条件

### 5.1 ベストの定義

ここでいう「ベスト」は、まず次の 2 条件を満たすものとする。

1. Dify に取り込めるコンテンツであること
2. Dify UI で取り込めること

その上で次に考える。

- 検索で引きやすいか
- 元文書の意味を壊しにくいか
- 運用しやすいか

### 5.2 今はまだ決めなくてよいこと

- 最終的な自動化方式
- Knowledge API 前提の投入設計
- 形式横断で完全に統一されたチャンキング方式
- 厳密なトークン上限や細かな実装パラメータ

## 5.3 現時点で追加確認できた前提

- Dify に投入する最終ファイル形式は、現時点では `Markdown (.md)` を第一候補とする
- 今回の主眼は「事前に完全なチャンク成果物を作って渡す」ことではなく、「Dify に渡した後、Dify 標準のチャンキングがうまく機能する形にファイルを整える」ことにある
- したがって、現時点では `1チャンク = 1ファイル` を前提にせず、`1ドキュメントを Dify に渡し、その中身を Dify 側で分割させる` 方向を基本認識とする
- 前処理の役割は、Dify が分割しやすく、かつ意味を壊しにくい構造へ文書を整形することに置く
- そのため、ここでいう「チャンキング検討」は、厳密には `Dify 標準チャンキングがうまく働く前処理形式の検討` を含む

## 6. 今後優先的に取り組むべき作業

この節は、今後の議論結果を反映するための運用用チェックリストとして扱う。
各項目は、議論して結論が出た時点で ` [x] ` に変更し、その直下に `決定事項` を追記する。

- [x] Dify UI の取り込み制約を確認する
  - 決定事項:
    - 最終投入形式は Markdown (.md) で確定
    - Dify のファイルサイズ上限は 15MB。100MB のファイルが 1 つあり物理分割が必要。他は 15MB 以内の見込み
    - Dify 自動チャンキングを使用。改行・段落区切り・見出しを手がかりに分割される前提
    - 見出しは Dify への区切りヒントとして使うが、見出し = 1 チャンクとは限らない。目標は「1 見出し配下 = 1 つの意味まとまりで、できれば 1 チャンク以内に収まるサイズ」
    - 意味のまとまりの基本単位は「1 つの表＋その前後の説明文」
    - 表は Markdown テーブルではなく「項目ラベル付き半構造化テキスト」に変換して渡す。行列の意味や制約・対応関係を壊さないことを優先
    - 物理分割は 15MB 超だけでなく、意味的に大きすぎる文書も対象。15MB は UI のハード制約、意味単位での分割は運用・検索品質の判断軸として別に持つ
    - 見出しの付与・保持方針は形式別検討で個別に決める
- [x] `Docs / Docx / Word` の代表サンプル文書を選ぶ
  - 決定事項:
    - Word 系は全て設計書・仕様書。1機能が表紙・変更履歴・仕様本文の別ファイルに分かれている
    - 変更履歴だけで 3〜4 ファイルに分かれるケースもある
    - 最重要は仕様本文だが、表紙・変更履歴・仕様本文の自動判別は現状できない
    - 全ファイル同じ扱いで入れる（C案）を採用。2TB 規模で手動分類は非現実的
    - 代表サンプルは特定ファイルではなく、仕様本文の典型構造をベースに方針を決める
- [x] Word 系で、文書の性質と壊してはいけない情報を整理する
  - 決定事項:
    - 壊してはいけない情報: 見出し階層、表の行列関係、階層化された表の親子関係、ワークフロー図の要素間関係（順序・分岐）、画面レイアウトのフィールド定義、プログラム番号・識別子、制約事項の記述、元ファイル名の追跡性
    - 仕様本文の構造は対象により多様: 見出し→説明→表の繰り返し、ワークフロー図、画面レイアウト、プログラム番号参照、階層化された表
- [x] Word 系で、最終出力を `自然文` `半構造化テキスト` `表保持` のどれに寄せるか決める
  - 決定事項:
    - 半構造化テキスト（B案）で確定
    - 見出し階層は `##` `###` で保持
    - 表は項目ラベル付き半構造化テキストに変換（第 1 段階で決定済み）
    - 説明文はそのまま残す
    - ワークフロー図・画面レイアウトはテキスト説明に変換を試みる。復元困難時はフォールバックする
- [x] Word 系で、Dify UI に投入する 1 ファイルの単位を決める
  - 決定事項:
    - 原則: 元ファイル 1 つ = Dify 1 document
    - 例外: 15MB 超または意味的に大きすぎる場合は章・節単位で子文書化する
    - 例外分割時の最小ルール: parent_file, part_no, split_reason を子文書に持たせる
- [x] Word 系の暫定チャンキング戦略を決める
  - 決定事項:
    - 正規化: `.doc` → `.docx` に変換
    - 構造抽出: 見出し・段落・表・図形を抽出し中間表現を保持
    - 整形: 半構造化テキストへ変換。見出し保持、表はラベル付きテキスト化、図はテキスト説明に変換
    - フォールバック: 表・図の復元が困難な場合は `LOW_CONFIDENCE` `TABLE_FALLBACK` 等の出力マーカーを付与し「要確認」として残す
    - 追跡情報: 相対パス、元拡張子、元ファイル名を最低限必須とする。中間成果物には doc_role 推定用の材料（先頭見出し、定型語等）も保持する
    - チャンキング: Dify 自動チャンキングを第一候補とする。実サンプルで検証し、表・制約事項の途中で切られる問題が出た場合は事前チャンク方式への切り替えを検討する
    - 物理分割: 15MB 超または意味的に大きすぎる場合は章・節単位で分割。例外時は parent_file, part_no, split_reason を付与
- [ ] 次に `RTF / TRF` を、Word へ正規化して扱う前提で検討する
  - 決定事項: 未記入
- [ ] その後に `Excel`、最後に `BAGLES` を検討する
  - 決定事項: 未記入

## 7. 確認すべき事実関係

### 7.1 Dify 側

- Dify UI が受け付けるファイル形式は何か
- Markdown をそのまま取り込めるか
- 1ファイルあたりの実運用上のサイズ上限はどの程度か
- 細かく分割済みのファイルを入れた場合、Dify 側で再分割されるか
- UI で手動投入する時、ファイル数が増えすぎると運用上どの程度厳しいか

補足:

- Dify 公式ドキュメントでは、各アップロード対象は `document` として扱われ、その後に `chunk` へ分割される
- したがって、現在の議論では「Dify に渡す 1 ファイル = 1 document」で考え、Dify 内部でどう chunk 化されるかを前提に整形方針を決める

### 7.2 文書側

- その形式の文書は、見出し中心か、表中心か、定義項目中心か
- 何を壊すと意味が失われるか
- 図、表、セルの対応関係、見出し構造などのうち、何を残す必要があるか
- どの単位なら「1つの意味のまとまり」と言えるか
- 自然文へ書き換えてよい形式か、構造を残した方がよい形式か

追加で分かっていること:

- 典型的な質問は、`エラーコードの意味`, `入力条件`, `その記述がどこに書かれているか` の確認である
- 影響調査として、`ここを修正したらどのドキュメントを修正すべきか`, `修正の影響範囲はどこか` を引きたい
- Word 系文書では、ワークフロー図の要素が多く、構造を読み違えるのが大きなリスクである
- 制約事項の誤読は避ける必要があり、単なる自然文化で意味を壊すことは避けたい
- 図や画像はテキスト説明に置き換えてよいが、その結果として文書構造が崩れるのは避けたい
- 表は可能なら表のまま残したいが、項目ラベル付きテキストへの変換も許容できる
- 一次文書との追跡性は最低限 `元ファイル名` が必須で、理想的には `ページ番号` や `表番号` まで追える状態が望ましい
- 更新運用は存在するが、現時点では手動での差し替えを前提とし、設計の最優先論点には置かない
- 文書は日本語中心だが、Bugles の識別番号、COBOL コード、エラーコード、プログラム番号などのコード要素を含む
- OCR が必要な文書は現時点では想定していない
- 最終評価基準は `Dify に登録できること` よりも `実際の質問で必要な情報が引けること` を上位に置く

### 7.3 Word 系を先に考える理由

- 実データの件数では `Docs / Docx / RTF / TRF` 系の比率が高い
- `RTF / TRF` は、現時点では Word へ変換してから扱う方針が有力である
- Word 系は、表、画像、テキストボックス、浮動オブジェクトなどが Markdown 化で崩れやすい
- そのため、Office 文書を直接 Markdown 化せず、先に構造を取る考え方を最初に検証する対象として適している

## 8. 検討が必要な事項

### 8.1 形式ごとに決めること

- どう抽出するか
- どう整形するか
- どういう単位で区切るか
- どういう形で Dify UI に渡すか

### 8.2 形式ごとの主な考え方

- `Excel`
  - レコード単位、表単位、シート単位のどれが自然かを考える
  - 完全な自然文より、項目ラベル付きの半構造化テキストが向く可能性がある

- `Docs / Docx / Word`
  - 見出し、節、段落の流れを残すべきかを考える
  - 一律に 1 文ずつへ書き下すと、文書構造が壊れる可能性がある
  - まずはこの形式群を最優先で検討する
  - 直接 Markdown に潰す前に、段落、見出し、表、画像、図形などの構造をどう保持するかを考える

- `RTF / TRF`
  - まず Word 系として正規化できるかを確認する
  - 正規化後も本文、表、図の関係が保てるかを見る
  - 検討順としては Word 系の次に置く

- `BAGLES`
  - 定義書の種類ごとに、どこを 1 単位として扱うべきかを考える
  - 文章化しすぎるより、項目名や定義種別を残した方がよい可能性がある
  - 検討順としては後段に置く

- `TXT / Log`
  - 見出し、段落、ログブロックのどこを 1 単位にするかを考える

## 9. 形式別にメモするときの観点

- その形式の典型的な文書構造
- 壊すと困る情報
- 意味の最小単位
- どこまで整形してよいか
- 最終出力は `自然文` `半構造化テキスト` `表保持` のどれがよいか
- Dify UI に投入する 1 ファイルの単位
- 品質確認の観点
- 未解決事項

## 10. 議論を進める順番

### 10.1 第1段階: Dify UI 前提の制約確認

1. Dify UI が受け付ける形式を確認する
2. 実運用上のファイルサイズ感とファイル数感を確認する
3. Markdown の扱いと、投入後の分割挙動を確認する
4. `1 document をどう整形すると Dify 側の chunking がうまく働くか` を確認する

### 10.2 第2段階: Word 系の方針決定

1. `doc / docx / docs` の代表サンプルを選ぶ
2. 何を壊すと困るかを整理する
3. 中間表現として何を保持するかを決める
4. Dify UI に入れる最終出力の形を決める
5. チャンク単位を `見出し / 節 / 表同梱` の観点で決める

### 10.3 第3段階: RTF / TRF の扱い整理

1. Word への正規化を前提にしてよいかを確認する
2. 正規化後に Word 系の方針へ合流できるかを確認する
3. 合流できない場合だけ、RTF / TRF 固有の補助ルートを考える

### 10.4 第4段階: Excel の扱い整理

1. レコード単位、表単位、シート単位のどれが自然かを決める
2. 自然文より半構造化テキストがよいかを確認する
3. 中間表現を経由すべきかを判断する

### 10.5 第5段階: BAGLES の扱い整理

1. 定義書種別ごとの区切り方を整理する
2. 項目名、定義種別、表構造をどこまで残すかを決める
3. Dify UI に入れる単位を決める

### 10.6 実装前に詳細化すべき事項

以下は暫定戦略の方向性は確定しているが、実サンプル検証後に詳細を固める必要がある項目である。

1. **メタデータ設計**: doc_role（cover/history/body）、revision_date、source_path、hash、group_id の定義。Knowledge API 移行時にまとめて設計する。ただし中間成果物には doc_role 推定材料を先行して保持する
2. **表の変換仕様**: 結合セル、空欄セルの継承、縦見出し、入れ子表、親子行の扱い。実サンプルで検証して決める。復元困難なケースはフォールバック対象とする
3. **図のフォールバック規則**: ワークフロー図・画面レイアウトについて、どこまで自動変換しどこで「要確認」に落とすか。図形内テキスト + 座標関係 + 接続順まで抽出し、解釈しきれないものはマーカー付きで残す方針
4. **Dify 自動チャンキングの検証**: 実サンプルで表・制約事項の途中切断が起きないか検証する。問題が出た場合は見出し/表/図の境界で事前チャンクする方式への切り替えを検討する
5. **変更履歴の重み分け**: 全ファイル同じ扱いで入れるが、検索時に古い仕様が混ざるリスクがある。「同じ扱いで入れる」と「同じ重みで使う」は分けて考える。API 移行時に doc_role で切り分ける設計を想定する
6. **構造抽出の技術選定**: `.doc` → `.docx` 変換は COM でよいが、`.docx` 本文の構造抽出まで COM に寄せると 2TB 規模で遅くて不安定になるリスクがある。実装段階では変換は COM、抽出は OpenXML 系の分離を検討する

## 11. 当面の進め方

1. 本文書（`docs/Task.md`）を設計判断の正本として扱う
2. `docs/ドキュメント処理パイプライン詳細設計書_v2.md` は設計ドラフトとして参照用に残す
3. `docs/拡張子別/*.md` に形式ごとの論点を整理する
4. まずは `Docs / Docx / Word` を最優先で考える
5. 次に `RTF / TRF` を Word 系へ接続する前提で考える
6. その後に `Excel`、最後に `BAGLES` を考える
7. 形式ごとの検討結果がそろってから、README や全体方針へ反映する

## 12. 参考リンクと今回の判断材料

### 12.1 Speaker Deck

- `RAG x FINDY`
  - https://speakerdeck.com/harumiweb/rag-findy
- 参照スライド
  - https://speakerdeck.com/harumiweb/rag-findy?slide=14

### 12.2 今回この資料から取り込みたい考え方

- Office 文書をそのまま Markdown やプレーンテキストに潰すのではなく、先に構造を取ってから再構成する
- まず中間表現を持ち、その後に最終出力を Dify UI へ投入しやすい形へ変換する
- Excel だけでなく、Word 系でも同じ発想を応用できる可能性が高い

### 12.3 Word 系で後追い確認するための公式リンク

- Paragraph object (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/word.paragraph
- Paragraphs object (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/word.paragraphs
- Tables object (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/word.tables
- Shape object (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/Word.Shape
- Shapes object (Word)
  - https://learn.microsoft.com/office/vba/api/Word.shapes
- InlineShape object (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/word.inlineshape
- InlineShapes object (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/Word.inlineshapes
- Range object (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/word.range
- Range.Information property (Word)
  - https://learn.microsoft.com/en-us/office/vba/api/word.range.information

### 12.4 上記リンクから今回確認したこと

- `Paragraph` / `Paragraphs` で段落単位を取得できる
- `Tables` で表を取得できる
- `InlineShape` / `InlineShapes` で本文中に埋め込まれた画像や OLE を扱える
- `Shape` / `Shapes` で浮動配置の図形やテキストボックスを扱える
- `Shape` はアンカーを持ち、`Range.Information` とあわせて位置や周辺文脈の補助情報を持てる
- したがって、Word 系は「直接 Markdown 化」より「構造抽出 -> 中間表現 -> 再構成」の方が検討価値が高い

### 12.5 Dify 公式ドキュメントで今回確認したこと

- アップロードした各ファイルは、Knowledge 内で `document` として扱われる
  - https://docs.dify.ai/en/use-dify/knowledge/manage-knowledge/maintain-knowledge-documents
- 各 document は chunk 設定に従って分割され、chunk が検索の基本単位になる
  - https://docs.dify.ai/en/use-dify/knowledge/create-knowledge/chunking-and-cleaning-text
- self-hosted では、単一ファイルサイズ上限や一括アップロード数を環境変数で調整できる
  - https://docs.dify.ai/en/use-dify/knowledge/create-knowledge/import-text-data/readme
- quick create では、アップロード後に chunk 設定をプレビューできる
  - https://docs.dify.ai/en/use-dify/knowledge/create-knowledge/introduction

## 13. 今の時点での結論

- 今の一律整形は、Dify 登録可否を試すためのベースラインとしては意味があった
- ただし、品質面では限界があり、形式ごとの設計へ進む必要がある
- 今後の議論では、まず「Dify に取り込める」「Dify UI で取り込める」を最優先条件とする
- その上で、形式ごとに最適な整形方法とチャンキング単位を考える
- 最初に検討する対象は `Docs / Docx / Word` とし、次に `RTF / TRF`、その後に `Excel`、最後に `BAGLES` を扱う
